{% extends 'base.html.twig' %}
{% block body %}
    <div class="row">
        {% for notice in app.flashes('notice') %}
            <span style="background: gold">{{ notice }}</span>
        {% endfor %}
    </div>
        {{ form(form) }}
{% endblock %}